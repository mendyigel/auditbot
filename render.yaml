services:
  - type: web
    name: auditbot
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: APP_URL
        value: https://orbiolab.com
    customDomains:
      - name: orbiolab.com
      - name: www.orbiolab.com
    autoDeploy: true
